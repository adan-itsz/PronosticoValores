const express = require('express')
const app = express();

app.get(/, (req,res) =>{
  const customers = [{

  }];
})

const port = 5000;

app.listen(port,() => console.log(`server activo en ${port}`));
